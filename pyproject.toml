[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "syscli"
version = "0.0.1"
description = "Simple colorful CLI library"
readme = "README.md"
authors = [{name = "waasaty"}]
dependencies = ["colorama"]
requires-python = ">=3.8"